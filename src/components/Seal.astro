---
interface Props { id?: string; text?: string; class?: string; spin?: boolean; }
const {
  id = 'seal',
  text = '• ALEGEREA VERDIKT • CÂȘTIGĂTOR • ',
  class: klass = '',
  spin = true,
} = Astro.props;
const pathId = `sealpath-${id}`;
---
<svg class={`seal ${spin ? 'spin' : ''} ${klass}`} viewBox="0 0 100 100" aria-hidden="true">
  <defs>
    <path id={pathId} d="M50,50 m-37,0 a37,37 0 1,1 74,0 a37,37 0 1,1 -74,0" />
  </defs>
  <circle cx="50" cy="50" r="47" fill="#fff" stroke="currentColor" stroke-width="1.5" />
  <text font-family="ui-monospace,monospace" font-size="8.4" font-weight="700" fill="currentColor" letter-spacing="1.4">
    <textPath href={`#${pathId}`}>{text}</textPath>
  </text>
  <path class="star" d="M50 37l3.2 6.6 7.2 1-5.2 5.1 1.2 7.2L50 60.5 43.4 64l1.2-7.2L39.4 51.7l7.2-1z" />
</svg>
